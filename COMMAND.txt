#powershell起動

#ディレクトリ移動(docker-compose.ymlのあるディレクトリに。)
cd D:\docker


#dockerコンテナ起動(ここでdocker-compose.ymlにしたがってコンテナ起動)
docker-compose up -d


#dockerコンテナに入る
docker exec -it --user root spe /bin/bash

#Splunk起動（Enterprise)
./app/splunk/bin/splunk start

Chromeからhttp://localhost:18000にアクセス（dockerの8000にバインドされる）




